# gRPC setup — TS + Rust clients, SubscribeRequest, commitment, ping/keepalive, x-token auth, provider endpoints

> Placeholder — authored at build checkpoint 3. See [SKILL.md](SKILL.md) for the router.
